#ifndef VECTOR_H
#define VECTOR_H

#include <stdlib.h>
#include <assert.h>
#include <utils.h>

extern void vector_normalize(float* v, size_t size);

extern void vector_sort(float* v, size_t size);

#endif // VECTOR_H

